apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: devops-cert

spec:
  secretName: devops-tls

  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

  dnsNames:
    - devops.sunilsinghrajput.in
    - api.devops.sunilsinghrajput.in
